server:
  port: 8080

spring:
  datasource:
    username:
    password:
    url:
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: update
    show-sql: true